---
all:
  children:
    mainnet_validators:
      children:
        remote:
          hosts:
            server1:
              ansible_host: <ip address>
            server2:
              ansible_host: <ip address>
              #any var from group vars:
        local:
          hosts:
            localhost:
              #any var from group vars:
              validator_name: "<neonevm_user>"